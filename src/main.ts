// エントリポイント。Task 10 で startApp を配線する。今は足場のプレースホルダ。
// GPU 初期化・RAF ループはここから呼ぶ予定（WebGPU 未対応なら案内を表示）。
export {};

const canvas = document.getElementById("app");
if (canvas) {
  // 実装が進むまでは何もしない（ビルドを通すためのスタブ）。
  console.info("image-evolution: scaffolding — engine 未配線");
}
